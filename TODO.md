1. Install initial dependencies of frontend which will be in react, react router, tailwindcss, Daisy ui
2. Project name will be Lost & Found
3. I would need a feedback system,
4. a home page which allows for selection of different levels
5. Each level is hot swappable with its own coordinates saved in the db
6. i would need a scoring system with at least 3 characters to find in each image
